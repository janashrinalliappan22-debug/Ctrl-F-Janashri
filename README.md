
  # CrowdSafe Prototype Development

  This is a code bundle for CrowdSafe Prototype Development. The original project is available at https://www.figma.com/design/gW9XVyzm5EIP0mjfESafId/CrowdSafe-Prototype-Development.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  